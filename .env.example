# AI Backend: "claude_cli" (default, uses `claude -p` subprocess) or "anthropic_api"
AI_BACKEND=claude_cli

# Only needed when AI_BACKEND=anthropic_api
ANTHROPIC_API_KEY=

# Claude model to use when using API (ignored for claude_cli)
CLAUDE_MODEL=claude-sonnet-4-6
